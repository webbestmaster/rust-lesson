fn main() {
    let result: Vec<i32> = two_sum(vec![1, 2, 3, 4], 5);
    assert_eq!(result, vec![1, 2]);

    println!("Success!");
}

pub fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> {
    for first in nums.iter() {
        pri
    }


    return vec![1, 2];
}